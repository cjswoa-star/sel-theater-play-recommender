# 추천 엔진 V2 기록

기준일: 2026-09-25

## 변경 이유
콘텐츠 FREEZE V1 이후 15개 실제 수업 시나리오를 현재 배포 코드로 실행했다. HARD 조건 위반과 후보 0건은 없었지만, 콘텐츠 보정 뒤에도 일부 추천 계산이 수정 전 `program_derived` 값을 계속 사용하는 문제가 확인됐다.

대표 사례는 015 `상상 물건 릴레이`이다. 프로그램에서 일반 교실·준비물 없음·낮은 이동 활동으로 수정했지만 기존 공간 호환성 파생값이 남아 3학년 일반 교실의 상상·비언어 조건에서 후보 밖으로 제외됐다.

## V2 변경
- 공간 HARD 필터와 공간 점수를 현재 `recommendation_normalized.space_types`에서 즉석 계산한다.
- 에너지 적합도를 현재 `activity_level`에서 계산한다.
- 안전 점수를 현재 `collision_risk`, `contact_level`, `movement_level`에서 계산한다.
- 교실 편의 점수를 현재 준비물·소음·이동 정보에서 계산한다.
- 다양성 그룹을 현재 참가 구조·이동량·첫 번째 목적 태그로 계산한다.
- 사용자가 활동 목적을 골랐고 목적 일치 후보가 3개 이상이면 그 후보군 안에서 최종 3개를 고른다.
- 정확 일치 후보가 3개 미만이면 기존 HARD 조건을 만족하는 유사 활동으로 채우되 추천 이유에 이를 밝힌다.
- 규칙 버전: 1.2 → 1.3

## 검증 결과
15개 시나리오 V1 테스트에서 후보 0건 0, HARD 위반 0이었다. 다만 015는 표적 시나리오에서 raw top10 밖이었다.

V2 시뮬레이션에서 015는 표적 시나리오 raw 1위가 되었고 3학년 일반 교실 상상·비언어·협력 시나리오의 실제 추천 3개 안에 포함됐다. 엔진 V2 생산 반영 후 별도 verifier에서 015 일반교실 HARD 통과, 표적 추천 포함, 3개 시나리오 HARD 위반 0, rule_version 1.3을 확인했다.

## 콘텐츠와의 관계
이 변경은 267개 놀이 원문을 다시 수정한 것이 아니다. `CONTENT_FREEZE_V1.md`의 콘텐츠 동결 상태는 유지한다. PDF 반영 범위에도 변화가 없다.

## 다음 단계
- 실제 공개 페이지에서 주요 조건 몇 가지를 수동 확인한다.
- 추천 결과 배열에 이상이 없으면 프로그램 추천 엔진도 V2로 고정한다.
- 이후 `PDF_SYNC_QUEUE.md` 기준으로 PDF를 부분 수정한다.
